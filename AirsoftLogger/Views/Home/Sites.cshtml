@model IEnumerable<AirsoftLogger.Models.Site>

@{
    ViewData["Title"] = "Sites";
}
<script src="~/lib/jquery/dist/jquery.js"></script>
<script src="~/js/Search.js"></script>
<script src="~/js/Scroll.js"></script>
<div class="siteHeading text-center">
    <h1>Sites</h1>
    <h2>On this page you'll find all the available sites stored within this website.</h2>
    <hr />
</div>

<div class="search text-md-center">

    <form asp-action="search" method="get">
        <h2> Search </h2>
        <label for="SearchString">Search For Site: </label> <input type="text" name="SearchString" id="SearchString" />
        <input type="submit" runat="server" value="filter" class="searchbutton" onclick="return searchcheck()" />
    </form>
</div>

@if (User.Identity.IsAuthenticated && User.IsInRole("Manager"))
{
    <div class="adminSiteadd">
        <a asp-controller="CMS" asp-action="CreateSite"> Add New <i fas fa-plus></i></a>
    </div>
}

<h2>Below is the list of all available sites </h2>


<table class="SiteTable table table-striped table-responsive">
    <thead class="thead-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SiteName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Website)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tel)
            </th>
            <th> Options</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.SiteName)
                </td>
                <td>
                    <a class="websitelink" href="@item.Website"> @Html.DisplayFor(modelItem => item.Website)</a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tel)
                </td>
                <td>
                    <a asp-controller="Home" asp-action="SiteDetails" asp-route-id="@item.SiteCode"> Details <i class="fas fa-info"></i></a>
                    @if (User.Identity.IsAuthenticated && User.IsInRole("Manager"))
                    {
                        <a asp-controller="CMS" asp-action="EditSite" asp-route-id="@item.SiteCode"> Update <i class="fas fa-pencil-alt"></i> </a>
                        <a asp-controller="CMS" asp-action="DeleteSite" asp-route-id="@item.SiteCode"> Delete <i class="fas fa-trash"></i></a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
<button onclick="toppage()" id="Top" title="Go to top">Top</button>